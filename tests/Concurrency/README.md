# tests/Concurrency

Real-process concurrency tests (`#[Group('concurrency')]`, committed state, `Tests\Support\ProcessPool`).
Module owners add `tests/Concurrency/<Module>/*Test.php` here (CONVENTIONS §6.5).
